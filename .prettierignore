# Generated by pnpm, never hand-formatted or reformatted by this repo's own lint.
pnpm-lock.yaml

# Prose this repository keeps as committed (AGENTS.md, README.md, CLAUDE.md): these
# predate the app moving into this repository and are not part of the app's own
# lint scope, the same way the canonry product repository's apps/landing package
# never had markdown files in its own `prettier --check .` scope either.
*.md
